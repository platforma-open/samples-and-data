
ll := import("@platforma-sdk/workflow-tengo:ll")
slices := import("@platforma-sdk/workflow-tengo:slices")
json := import("json")
util := import(":util")

// TODO implement
normalizeString := func(str) {
	return str
}

// Collect unique values of a map (Record<sampleId, value>), sorted.
// Sorted output keeps the encoded spec stable across runs (Tengo map
// iteration is non-deterministic) and yields scannable dropdowns.
collectUniqueValues := func(dataMap) {
	seen := {}
	unique := []
	for _, v in dataMap {
		key := string(v)
		if !seen[key] {
			seen[key] = true
			unique = append(unique, v)
		}
	}
	return slices.quickSort(unique)
}

/**
 * Adds metadata columns to the exports
 *
 * @param args - arguments
 * @param blockId - block id
 * @param sampleIdAxisSpec - sample id axis spec
 * @param groupIdAxisSpec - sample group id axis spec
 * @return columns - metadata columns (id -> {spec, data})
 */
createMetadata := func(args, blockId, sampleIdAxisSpec) {
    columns := {}
	for _, column in args.metadata {
		annotations := {
			"pl7.app/label": column.label
		}

		// Mark String metadata columns as discrete filters and attach their
		// observed categories so downstream blocks (e.g. Lead Selection) can
		// render dropdowns for filtering on this variable.
		// Cap at 500: larger cardinalities are usually identifier-like
		// (barcodes, free-text notes) and would bloat the PFrame spec
		// without producing a usable dropdown.
		if column.valueType == "String" {
			uniqueValues := collectUniqueValues(column.data)
			if len(uniqueValues) > 0 && len(uniqueValues) <= 500 {
				annotations["pl7.app/isDiscreteFilter"] = "true"
				annotations["pl7.app/discreteValues"] = string(json.encode(uniqueValues))
			}
		}

		columns["metadata." + column.id] = {
			spec: {
				kind: "PColumn",
				name: "pl7.app/metadata",
				valueType: column.valueType,
				annotations: annotations,
				domain: {
					"pl7.app/columnId": column.global ? normalizeString(column.label) : column.id
				},
				axesSpec: [sampleIdAxisSpec]
			},
			data: util.createJsonPColumnData(column.data)
		}
	}

	//
	// Creating sample label column
	//

	columns["metadata.sampleId"] = {
		spec: {
			kind: "PColumn",
			name: "pl7.app/label",
			valueType: "String",
			annotations: {
				"pl7.app/label": args.sampleLabelColumnLabel,
				"pl7.app/isLabel": "true"
			},
			axesSpec: [sampleIdAxisSpec]
		},
		data: util.createJsonPColumnData(args.sampleLabels)
	}

    return columns
}

export {
    createMetadata: createMetadata
}