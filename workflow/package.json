{
  "name": "@platforma-open/milaboratories.samples-and-data.workflow",
  "version": "2.5.0",
  "type": "module",
  "files": [
    "./dist/**/*"
  ],
  "description": "Tengo-based template",
  "scripts": {
    "build": "shx rm -rf dist/* && pl-tengo check && pl-tengo build",
    "format": "/usr/bin/env emacs --script ./format.el"
  },
  "dependencies": {
    "@platforma-open/milaboratories.samples-and-data.parse-h5ad": "workspace:*",
    "@platforma-open/milaboratories.samples-and-data.parse-seurat": "workspace:*",
    "@platforma-sdk/workflow-tengo": "catalog:"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:"
  }
}
