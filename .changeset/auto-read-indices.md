---
'@platforma-open/milaboratories.samples-and-data.ui': patch
---

Add helper to collect read indices from manual patterns and automatically sync `readIndices` when the compiled pattern changes in the import dialog.

