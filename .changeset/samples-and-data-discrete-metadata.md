---
"@platforma-open/milaboratories.samples-and-data.workflow": minor
"@platforma-open/milaboratories.samples-and-data": minor
---

Annotate String metadata columns with `pl7.app/isDiscreteFilter` and `pl7.app/discreteValues` so downstream blocks (Lead Selection, Clonotype Distribution) can render them as dropdown filters instead of free-text fields.
