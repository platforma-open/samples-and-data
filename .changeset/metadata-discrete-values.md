---
"@platforma-open/milaboratories.samples-and-data": minor
"@platforma-open/milaboratories.samples-and-data.workflow": patch
---

Annotate every metadata column with `pl7.app/isDiscreteFilter` and `pl7.app/discreteValues`. Downstream blocks can offer the column as a discrete filter or a grouping property without a read of its data.
